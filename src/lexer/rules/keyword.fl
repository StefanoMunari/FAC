{WHILE}															{ return (WHILE); }
{IF}															{ return (IF); }
{ELSE}															{ return (ELSE); }
{SKIP}															{ return (SKIP); }
{PRINT}/{SPACE}*{L_DEL_EXPR}{SPACE}*{ID}{SPACE}*{R_DEL_EXPR}	{ return (PRINT); }